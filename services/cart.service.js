const Cart = require("@models/cart");
const AppError = require("@utils/appError");

const addItem = async (userId, slug, quantity) => {};

const updateItem = async (userId, slug, quantity) => {};

module.exports = {
    addItem,
    updateItem
};
